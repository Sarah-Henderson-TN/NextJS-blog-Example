---
title: "When to use static generation vs Server Side Rendering (SSR)"
date: "2026-04-16"
---

We recommend using **Static Generation** (with and without data) whenever possible.

This is because your page can be built and served by CDN, which makes it much faster than having to have a server render the page on every request.
